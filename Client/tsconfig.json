{
  "compilerOptions": {
    "target": "es6",       // Target ECMAScript version
    "module": "commonjs",  // Module system
    "outDir": "./dist",
         // Folder to store compiled JavaScript
  },
  "include": ["src/**/*.ts"]  // Include all TypeScript files in the `src` folder
}
